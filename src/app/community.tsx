import { ComingSoonScreen } from '@/components/coming-soon-screen';

/**
 * Community tab root (the 5th tab, per Community-Architecture-Amendment-002 and the
 * handoff screen). The real screen is Community Home.dc.html (Phase 3, STEP D).
 */
export default function CommunityScreen() {
  return <ComingSoonScreen title="Community" />;
}
