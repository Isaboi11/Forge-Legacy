import { ComingSoonScreen } from '@/components/coming-soon-screen';

/**
 * Workouts tab root (plural). The real screen is W-2 Program Browse / the Programs
 * Catalog (Phase 3, STEP D). Distinct from `/workout` (singular) — the active
 * workout session that Home's "Start Workout" pushes to.
 */
export default function WorkoutsScreen() {
  return <ComingSoonScreen title="Workouts" />;
}
